# 每月一次
monthly_country_pulse = {
    effect = {
        if = {
            limit = { tag = MNG }
            generate_character_create_effect_mingchu = yes
            tiandao_auto_marry = yes
        }
    }
}